#include<stdio.h>
int main()
{
	char s[20], s2[20];
	 
	 printf("enter string1 :");
	 gets(s);
	 printf("enter string2:");
	 gets(s2);
	  
	  printf("join strings=%s%s",s,s2);
	  
	  
}
